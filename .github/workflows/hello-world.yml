name: GitHub Actions Demo
on: push
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
      - uses: gradle/gradle-build-action@v2
        with:
         gradle-version: 8.5
      - run: make say-hello
       
       
